# Install: data-analysis

This skill wraps the standalone `prop-hedge-agents` project. The skill body references
the project via the `$PROP_HEDGE_AGENTS_HOME` env var, with a default of `~/projects/prop-hedge-agents`.

## Setup

```bash
# 1. Clone the project (skip if you already have it)
git clone https://github.com/JansenAnalytics/prop-hedge-agents.git ~/projects/prop-hedge-agents

# 2. Install dependencies
cd ~/projects/prop-hedge-agents && npm install

# 3. (Optional) Pin the location with an env var
export PROP_HEDGE_AGENTS_HOME=~/projects/prop-hedge-agents
# Add the above to ~/.bashrc or ~/.zshrc to make it permanent.
```

## What the skill expects

- See the skill body for the exact entry points referenced.

## Verification

```bash
ls "${PROP_HEDGE_AGENTS_HOME:-$HOME/projects/prop-hedge-agents}"
```

If the directory exists, the skill should work. If not, run the Setup steps above.

## Notes

- The skill body uses `${PROP_HEDGE_AGENTS_HOME:-$HOME/projects/prop-hedge-agents}` — both env-var and default-path paths work.
- This INSTALL.md was generated by `scripts/migrate-track-b.sh`. Edit freely if the auto-detected entries are wrong.
